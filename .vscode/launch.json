{
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${fileDirname}",
            "env": {
                "AUTH_SERVICE_MONGO_URI": "mongodb://root:root@localhost:27017/",
                "AUTH_SERVICE_MONGO_DB_NAME": "TestDb"
            }
        }
    ]
}